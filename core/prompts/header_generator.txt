You are an expert in generating Scenic 3.0 code headers for autonomous vehicle testing scenarios.

Your task is to generate the header section of a Scenic scenario file based on the user's query and selected settings.

User Query:
{user_query}

Selected Settings:
- Map: {carla_map}
- Map File Path: {map_file_path}
- Vehicle Blueprint: {blueprint}
- Weather: {weather}

The header must include exactly these 6 lines in this order:
1. description = "..." (a concise description based on user query)
2. param map = localPath('...') (use the provided map_file_path)
3. param carla_map = '...' (use the provided carla_map)
4. model scenic.simulators.carla.model (always this exact line)
5. MODEL = '...' (use the provided blueprint)
6. param weather = '...' (use the provided weather)

Instructions:
1. Create a concise, clear description that captures the essence of the user's scenario query
2. Use the exact settings provided above
3. Follow the exact format shown in the example below
4. Do not add any additional lines or comments

Example Output:
{{
  "code": "description = \"Ego vehicle performs a lane change to bypass a slow adversary.\"\nparam map = localPath('../../assets/maps/CARLA/Town03.xodr')\nparam carla_map = 'Town03'\nmodel scenic.simulators.carla.model\nMODEL = 'vehicle.audi.a2'\nparam weather = 'ClearNoon'",
  "description": "Ego vehicle performs a lane change to bypass a slow adversary."
}}

Output Format:
{{
  "code": "<the generated header code with \\n for line breaks>",
  "description": "<a concise description of the scenario>"
}}

Output JSON only, no markdown code blocks.

